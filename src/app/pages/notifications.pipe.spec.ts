import { NotificationsPipe } from './notifications.pipe';

describe('NotificationsPipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationsPipe();
    expect(pipe).toBeTruthy();
  });
});
